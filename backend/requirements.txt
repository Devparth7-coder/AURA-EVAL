# AURA-EVAL backend — core runtime dependencies.
#
# Kept deliberately lean so this file can be installed directly by
# @vercel/python in the two-project deploy (Root Directory = backend) without
# exceeding the 250 MB unzipped lambda limit.
#
# Heavy, strictly-optional extras (Parquet export, Redis queue) live in
# requirements-optional.txt and degrade gracefully when absent:
#   * no pyarrow -> Parquet export is disabled; JSON/JSONL/CSV still work
#   * no redis   -> TaskQueue falls back to the inline/QStash provider

# --- core -------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
pydantic-settings==2.7.1
python-dotenv==1.0.1
httpx==0.28.1

# --- orchestration ----------------------------------------------------
langgraph==0.2.62
langchain-core==0.3.29

# --- database ---------------------------------------------------------
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
